@model IEnumerable<Hotel.BLL.DTO.ConvenienceDTO>

@{
    ViewBag.Title = "List";
}

<h2>List</h2>

<p>
    @Html.ActionLink("Create New", "Create", new { controller = "Convenience"}, new { @class = "btn btn-success" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { controller="Convenience", id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { controller = "Convenience", id =item.Id })
        </td>
    </tr>
}

</table>
